# Only needed for authenticated endpoints; public market data works without them.
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Comma-separated list of symbols to watch
SENTINEL_SYMBOLS=BTCUSDT,ETHUSDT

SENTINEL_LOG_LEVEL=INFO
